# EarningsCallSentiment

A starter workspace for Earnings Call Sentiment analysis.

## Project setup

- Create a Python virtual environment.
- Install dependencies with `python -m pip install -U pip`.
- Install project requirements if added later.

## Run

```powershell
python -m earnings_call_sentiment.main
```

## Testing

```powershell
python -m pytest
```
